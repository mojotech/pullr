mocha   = require 'mocha'
assert  = require 'assert'
pullr   = require '../bin/pullr'

console.log "h"
describe "opening a PR", () ->
  it "should send a valid message to github", () ->

    throw "fuck"
